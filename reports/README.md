# Generated reports (snapshot)

These HTML files are the Evinced output from a local run, committed for reference
so the results can be viewed without running the suite. Open any of them directly
in a browser.

| File | Source test | Mode | Issues |
|------|-------------|------|--------|
| `A-home-navigation.html` | `tests/home.navigation.spec.js` | on-demand (`evAnalyze`) | 17 |
| `B-booking-flow.html` | `tests/booking.flow.spec.js` | continuous (`evStart`/`evStop`) | 19 |
| `aggregatedReport.html` | both, merged by the Evinced reporter | - | 24 unique |

Regenerate with `npm test` (output goes to `evincedReports/`, which is gitignored).
